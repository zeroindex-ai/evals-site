@AGENTS.md

## Claude Code specifics

- **Skills** — design/chrome edits: `zeroindex-app-layout` (apex chrome lives in the
  `zeroindex-site` repo, not here). New repos: `scaffold-zeroindex-service` / `-package`.
- **Hooks** — `md-review-gate` blocks committing unsanitized public-repo `.md`.
- **MCP** — none.
